// Forge · kit Vue — ré-export du nommage centralisé. `engine/brand.ts` est un
// module de CONSTANTES PURES (zéro dépendance, pas de Hono) → importable par le
// bundle front sans tirer le moteur. Source unique : voir engine/brand.ts.

export * from "../engine/brand.ts"
